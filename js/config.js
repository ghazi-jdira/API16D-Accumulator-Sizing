/* Frontend configuration. Edit this for your deployment.
 *
 * apiBase - the public URL of your Render backend, no trailing slash.
 *           e.g. "https://api16d-backend.onrender.com"
 *
 * This is NOT a secret: the API URL is public by design. Access is controlled
 * by the username/password login enforced on the server; the confidential data
 * and formulas live on the backend and are never sent to the browser.
 */
window.APP_CONFIG = {
  apiBase: "https://api16d-backend.onrender.com",
};
